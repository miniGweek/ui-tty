# Design

Design docs record how a feature or module will be implemented when the approach needs agreement before code.

Use a design doc for meaningful implementation trade-offs, interface choices, state ownership, sequencing inside a slice, or compatibility constraints. Keep user-visible behavior in specs and system-wide decisions in architecture docs.
