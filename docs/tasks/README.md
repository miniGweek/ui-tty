# Task Contracts

Task contracts define one executable unit of work.

A valid task contract states the source outcome, inputs, expected output, dependencies, acceptance checks, likely files or modules, and verification evidence. The implementer should not need to infer scope.
