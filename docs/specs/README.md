# Specs

Specs define behavior that needs precision before implementation.

Use a spec when a task depends on user-visible behavior, APIs, data contracts, states, errors, permissions, or edge cases that would otherwise be guessed during execution.
